// import { AppAction } from "../reducer";
// export const renderJSX = (
//   renderList: string[],
//   insertHtml: (name: string, index: number) => JSX.Element,
//   isSortTime: boolean,
//   isSortAlphabet: boolean) => {
//   if (isSortTime) {
//     let sortRenderList = renderList.reverse();
//     if()
//       AppAction.
//     return .map((name, index) =>
//     insertHtml(name, index));
//   }
//   if (isSortAlphabet)
//     return renderList.sort().map((name, index) =>
//       insertHtml(name, index));
// };
